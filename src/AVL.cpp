#include "AVL.h"
#include <iostream>

#include <string.h>
#include <fstream>
#include <cstdlib>
#include <conio.h>
#include <cstdio>
#include <sstream>
#include <math.h>

using namespace std;



/*
 * Inorder Traversal of AVL<T> Tree //
 */
 /*
template <class T>
void AVL<T>::inorder(ofstream& output,page *node )
{

    if (node != NULL)
    {
        inorder(output,node->left);
        output<<node->data<<","<<"number of links: "<<MBbinarytree_count_recursive(node->Link)<<",links:";
        internal_inorder(output,node->Link);
        output<<endl;
        inorder(output,node->right);
    }

}
*/

